# Keep the contributor toolchain lean. CI adds Apple and Windows targets as
# staged smoke builds while Linux ARM and Windows ARM remain future work.
[toolchain]
channel = "1.94.0"
profile = "minimal"
components = ["rustfmt", "clippy"]
targets = ["x86_64-unknown-linux-musl", "x86_64-unknown-linux-gnu"]
