# Validation Report: Installable CLI Distribution and Release UX

## Status

Implementation is complete at the repository level. Structural validation,
shared-skill validation, and a workflow-aligned local release dry run all
passed. Public release publication remains blocked pending a distinct human
release-readiness review on native GitHub-hosted artifacts.

## Checklist Status

| Checklist | Total | Completed | Incomplete | Status |
| --- | --- | --- | --- | --- |
| `requirements.md` | 20 | 20 | 0 | PASS |

Implementation proceeded automatically because all checklist items were already
complete.

## Phase Execution Tracking

| Phase | Scope | Status | Evidence Path |
| --- | --- | --- | --- |
| Phase 0 | Governance and artifact alignment | Complete | `specs/005-cli-release-ux/decision-log.md` |
| Phase 1 | Release scaffolding and script entrypoints | Complete | `scripts/release/`, `.github/release-notes-template.md` |
| Phase 2 | Shared naming, matrix, and parity scaffolding | Complete | `.github/workflows/ci.yml`, release contracts |
| US1 | Install-first docs and walkthroughs | Complete | `README.md`, `specs/001-canon-spec/quickstart.md`, `specs/005-cli-release-ux/quickstart.md` |
| US2 | Packaging and release workflow | Complete | `scripts/release/`, `.github/workflows/release.yml` |
| US3 | Skill recovery guidance | Complete | `.agents/skills/canon-shared/references/runtime-compatibility.toml` |
| Final | Validation and independent review | In progress | this file |

Planned validation for this feature covers:

- release workflow structure and artifact completeness
- version-surface alignment across all public release surfaces
- install-first documentation posture in README and quickstart
- cross-platform install walkthroughs using published artifacts
- Canon skill recovery behavior when `canon` is missing, incompatible, or
  shadowed on PATH

## Implementation Evidence Tables

### Artifact Completeness Evidence

| Artifact | Expected | Observed | Result | Notes |
| --- | --- | --- | --- | --- |
| `canon-<VERSION>-macos-arm64.tar.gz` | required | `canon-0.5.0-macos-arm64.tar.gz` | PASS | present in workflow-aligned local dry run at `.canon/tmp/release-dry-run` |
| `canon-<VERSION>-macos-x86_64.tar.gz` | required | `canon-0.5.0-macos-x86_64.tar.gz` | PASS | local dry run validated naming, archive shape, checksum entry, and version sidecar |
| `canon-<VERSION>-linux-arm64.tar.gz` | required | `canon-0.5.0-linux-arm64.tar.gz` | PASS | local dry run validated naming, archive shape, checksum entry, and version sidecar |
| `canon-<VERSION>-linux-x86_64.tar.gz` | required | `canon-0.5.0-linux-x86_64.tar.gz` | PASS | local dry run validated naming, archive shape, checksum entry, and version sidecar |
| `canon-<VERSION>-windows-x86_64.zip` | required | `canon-0.5.0-windows-x86_64.zip` | PASS | local dry run validated naming, archive shape, checksum entry, and version sidecar |
| `canon-<VERSION>-SHA256SUMS.txt` | required | `canon-0.5.0-SHA256SUMS.txt` | PASS | generated by `scripts/release/verify-release-surface.sh --write-checksums` |

### Version Surface Evidence

| Surface | Expected Version | Observed | Result | Notes |
| --- | --- | --- | --- | --- |
| `Cargo.toml` workspace version | `0.5.0` | `0.5.0` | PASS | verified during structural validation and local dry run metadata render |
| Rendered release notes | `0.5.0` | `Canon 0.5.0` and `Tag: v0.5.0` | PASS | rendered into `.canon/tmp/release-dry-run/release-notes.md` |
| Archive names | `0.5.0` | all six release-surface files are versioned `0.5.0` | PASS | verified by `scripts/release/verify-release-surface.sh` |
| Checksum manifest name and entries | `0.5.0` | `canon-0.5.0-SHA256SUMS.txt` with one entry per archive | PASS | verified by `scripts/release/verify-release-surface.sh` |
| `canon --version` output per built binary | `0.5.0` | `canon 0.5.0` | PASS | native host binary executed locally; release workflow enforces per-artifact version sidecars |
| Shared compatibility reference | `0.5.0` | `expected_workspace_version = "0.5.0"` | PASS | release-based install/update guidance added without changing expected version |

### Install Walkthrough Evidence

| Platform | Artifact | PATH Location | Version Check | `canon init` Smoke Test | Result | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| macOS arm64 | `canon-0.5.0-macos-arm64.tar.gz` | `/Users/rt/workspace/apply-the/canon/.canon/tmp/release-dry-run/install-bin/canon` | `canon 0.5.0` | success with `methods_materialized=12`, `policies_materialized=5` | PASS | local native-host install smoke from workflow-aligned dry run |
| macOS x86_64 | `canon-0.5.0-macos-x86_64.tar.gz` | pending native machine | pending native machine | pending native machine | BLOCKED | local dry run validated archive layout and parity, not native execution |
| Linux arm64 | `canon-0.5.0-linux-arm64.tar.gz` | pending native machine | pending native machine | pending native machine | BLOCKED | local dry run validated archive layout and parity, not native execution |
| Linux x86_64 | `canon-0.5.0-linux-x86_64.tar.gz` | pending native machine | pending native machine | pending native machine | BLOCKED | local dry run validated archive layout and parity, not native execution |
| Windows x86_64 | `canon-0.5.0-windows-x86_64.zip` | pending native machine | pending native machine | pending native machine | BLOCKED | local dry run validated archive layout and parity, not Windows-native execution |

### Skill Recovery Evidence

| Scenario | Expected Result | Observed | Result | Notes |
| --- | --- | --- | --- | --- |
| `cli-missing` | release-based install guidance | `STATUS=cli-missing` with README and Releases URLs in `ACTION` | PASS | validated in Bash after removing Canon from PATH |
| `version-incompatible` | release-based update guidance | `STATUS=version-incompatible` for fake `canon 0.3.0` shim | PASS | validated in Bash with an incompatible shim earlier on PATH |
| PATH-shadowed binary | actionable troubleshooting | `STATUS=version-incompatible` when older shim precedes valid Canon on PATH | PASS | README and quickstart now direct the user to rerun `canon --version` and inspect PATH resolution |
| valid installed Canon | `STATUS=ready` | `STATUS=ready`, `VERSION_KIND=semver`, `DETECTED_VERSION=0.5.0` | PASS | Bash helper contract unchanged for success path |

## Validation Ownership

- generation side:
  - release workflow and packaging scripts
  - documentation updates
  - runtime-compatibility reference updates
- validation side:
  - CI structural checks for workflow outputs and artifact names
  - fresh-environment install walkthrough reviewer
  - Bash and PowerShell skill-validator runs
  - independent release-readiness reviewer distinct from the asset producer

## Structural Validation

Planned checks:

- `cargo fmt --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --locked`
- `cargo nextest run --locked`
- release-workflow dry run against a release candidate tag or manual dispatch
- artifact inventory check against the release contract
- archive-content check for `canon` and `canon.exe`
- checksum manifest completeness and formatting check
- `git diff --check`
- `scripts/validate-canon-skills.sh`
- `pwsh -File scripts/validate-canon-skills.ps1`

Execution notes:

- use local command execution for formatting, tests, and skill validators
- use the release scripts plus the release workflow definition for dry-run
  verification when GitHub-hosted execution is unavailable locally
- keep human independent review separate from generation; if a human reviewer
  is unavailable during local implementation, capture the blocker explicitly

Expected assertions:

- exactly five public platform archives are produced per release
- exactly one versioned SHA256 manifest is produced per release
- every archive name matches `canon-<VERSION>-<os>-<arch>.<ext>`
- every checksum entry maps to a published archive name
- README and quickstart surfaces lead with binary installation and do not use
  Cargo in normal daily-use examples
- shared skill compatibility guidance points to release-based installation

## Release Artifact Validation Matrix

| Artifact | Expected Format | Expected Binary | Validation |
| --- | --- | --- | --- |
| `canon-<VERSION>-macos-arm64.tar.gz` | `tar.gz` | `canon` | archive opens, checksum matches, version output matches |
| `canon-<VERSION>-macos-x86_64.tar.gz` | `tar.gz` | `canon` | archive opens, checksum matches, version output matches |
| `canon-<VERSION>-linux-arm64.tar.gz` | `tar.gz` | `canon` | archive opens, checksum matches, version output matches |
| `canon-<VERSION>-linux-x86_64.tar.gz` | `tar.gz` | `canon` | archive opens, checksum matches, version output matches |
| `canon-<VERSION>-windows-x86_64.zip` | `zip` | `canon.exe` | archive opens, checksum matches, version output matches |
| `canon-<VERSION>-SHA256SUMS.txt` | text | N/A | every archive covered exactly once |

## Version Visibility Validation

The following surfaces must match semver exactly for a release candidate:

- `Cargo.toml` workspace version
- Git tag used for release publication
- release note title and install snippets
- archive names
- checksum manifest file name and entries
- `canon --version` output from every built binary
- `.agents/skills/canon-shared/references/runtime-compatibility.toml`

Any mismatch blocks release publication.

## Structural Validation Results

| Check | Status | Evidence |
| --- | --- | --- |
| `cargo fmt --check` | PASS | exited cleanly after release UX changes |
| `cargo clippy --all-targets --all-features -- -D warnings` | PASS | exited cleanly across the workspace |
| `cargo test --locked` | PASS | exited cleanly |
| `cargo nextest run --locked` | PASS | 30 tests passed |
| `git diff --check` | PASS | trailing whitespace fixed in Speckit templates before rerun |
| `scripts/validate-canon-skills.sh` | PASS | returned `PASS: Canon skill structure, support-state labels, overlap boundaries, and fake-run protections are valid.` |
| `scripts/validate-canon-skills.ps1` | PASS | returned `PASS: Canon skill structure, support-state labels, overlap boundaries, and fake-run protections are valid.` |

## Install Walkthrough Validation

### Unix Platforms

Required walkthroughs:

- macOS arm64 install into a user PATH directory
- macOS x86_64 install into a user or system PATH directory
- Linux arm64 install into a user or system PATH directory
- Linux x86_64 install into a user or system PATH directory

For each walkthrough, record:

- artifact selected
- checksum verification output
- extraction step
- final PATH location
- `canon --version` output
- `command -v canon` output
- `canon init --output json` result in a fresh Git repo

### Windows Platform

Required walkthrough:

- Windows x86_64 install into `%USERPROFILE%\bin` or another documented PATH
  directory

For the walkthrough, record:

- artifact selected
- checksum verification output
- extraction and copy step
- resulting PATH entry
- `canon --version` output
- `Get-Command canon` output
- `canon init --output json` result in a fresh Git repo

## Skill Compatibility Validation

Required cases:

- `cli-missing` points to release-based install guidance
- `version-incompatible` points to release-based update guidance
- PATH-shadowed binary troubleshooting is documented and actionable
- success path still reports `STATUS=ready` when a valid Canon binary is on
  PATH
- Bash and PowerShell behavior remain materially aligned

Evidence to capture:

- helper output for missing Canon
- helper output for incompatible Canon
- helper output for ready Canon
- screenshots or transcripts of the README install guidance referenced by the
  helper

## Independent Review Record

| Reviewer | Scope | Status | Findings |
| --- | --- | --- | --- |
| Copilot Explore read-only review | workflows, packaging scripts, install docs, compatibility guidance | PASS with publication blocker retained | no blocking code findings after triage; human release-owner review on native/public artifacts still required before publication |

## Independent Validation Focus

Independent review must confirm:

- the shipped asset set matches the documented platform matrix exactly
- published install guidance leads with prebuilt binary installation rather
  than source build
- a fresh user can install and verify Canon without invoking Cargo
- version strings are consistent across release notes, archives, and CLI output
- skill recovery guidance does not instruct the user to build from source for
  daily use

Independent review outcome fields to fill during implementation:

- reviewer name
- review date
- pass or fail by validation layer
- blocking issues and required follow-up

## Evidence Artifacts

Record or link the following under this feature directory during
implementation:

- release asset inventory
- checksum manifest copy or transcript
- install walkthrough transcripts per supported platform
- `canon --version` transcripts from each artifact
- PATH resolution transcripts
- skill helper output transcripts for missing and incompatible Canon
- release-readiness signoff notes

## Blocking Notes

- public release publication remains blocked until a distinct human reviewer validates native GitHub-hosted artifacts and signs off on release readiness
- the local dry run under `.canon/tmp/release-dry-run` used the native host binary as a stand-in for non-native archives to validate naming, archive layout, checksum coverage, rendered notes, and PATH-based install smoke behavior
- `cargo deny check licenses advisories bans sources` passed, with non-blocking `license-not-encountered` warnings for existing allowlist entries in `deny.toml`