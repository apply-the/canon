version = 1
# Requires explicit `--system-context existing` when starting a run.
mode = "refactor"
emphasis = "execution-heavy"
implementation_depth = "full"
gate_profile = ["ChangePreservation", "Architecture", "Risk", "ReleaseReadiness"]
artifacts = [
    "preserved-behavior.md",
    "refactor-scope.md",
    "structural-rationale.md",
    "regression-evidence.md",
    "contract-drift-check.md",
    "no-feature-addition.md",
]
